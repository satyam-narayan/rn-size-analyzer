rn-size-check:
  image: node:20
  script:
    - npx rn-size-analyzer analyze --format json --out rn-size-report
    - npx rn-size-analyzer check --json
  artifacts:
    when: always
    paths:
      - rn-size-report
